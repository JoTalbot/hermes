# MILESTONE — 2026-09-22: волна 2026-09-19 зафиксирована в git

**Узел:** `arm-server-01` (129.213.177.56) · Ubuntu 24.04.4 aarch64 · 23 GiB RAM · диск 52 % занято (71 ГБ свободно)
**База:** `4b1897e` (2026-09-17, четвёртая волна) · на входе — 11 незакоммиченных записей (7 изменённых, 1 удалённый, 3 неотслеживаемых), датированы 19–20.09
**Итог:** `tests/run.sh` **307 passed · 0 failed · 0 skipped** (было 290 · 2 · 0) · `agents-selftest` **51/0** (было 49/2) · secret-scan clean
**Страховочный архив до правок:** `/home/ubuntu/hermes-wave-20260922/` (pre-state patch, HEAD, копии снятых `.bak`, pre-версии юнита и тестов)

---

## 1. Что было не так на входе (4 красных гейта)

| Гейт | Ожидалось | Было |
|---|---|---|
| `[12] agents selftest` | `FAIL=0` | `PASS=49 FAIL=2` (каскад с двух следующих) |
| live: политика моделей загружается | `MODEL-POLICY: OK` | `MODEL-POLICY: FAIL` |
| selftest `[4]` «рутина — дешёвый тир» | `routine=hermes-fast` | проба печатала только `agents/uncovered/bad_tier` |
| selftest `[5]` «шим отдаёт шесть тиров» | `6` | `7` |

Причины:

1. `config/models.yaml` был затёрт содержимым сценария «локально-первая политика»: файл сам о себе писал «НЕ АКТИВЕН» и не содержал обязательного раздела `escalate` — установщик политики (`scripts/install-model-policy.sh --check`) справедливо отказывал, а тест «рутина — дешёвый тир» показывал, что простые агенты уехали на локальные модели.
2. В шим был добавлен тир `arena`, которого балансер не знает.

## 2. Что сделано

### 2.1 Правки волны 19.09 зафиксированы (они уже работали на узле, но не были в git)

- `agents/runtime.py` — **упоминание срезается до разбора обработчика** («@node-arm-llm/server-guardian ask …» разбиралось как обработчик `@node-arm-llm/server-guardian`, которого нет: разговор агентов в общем канале не работал); `analysis` больше не константа (`or True` заставлял каждый `ask` эскалировать в `hermes-reason`, из-за чего назначенный тир был недостижим); `summary` не подменяется заметкой, когда модель промолчала.
- `agents/routing.py` — `agent.handlers` это **словарь** `{имя: [обработчики]}`, а не список: `handlers[0]` давал `KeyError: 0` на каждом `ask` у агента без `status` (monitoring: health/alerts/targets).
- `agents/models.py` — для тира `local` факты режутся до 900 знаков: замер 19.09 (комментарий автора правки) — промпт 5707 токенов на qwen2.5:3b / 4 ядра ARM = 173,6 с в первый проход и 0,5 с по кэшу промпта, то есть полный объём уходит в таймаут.
- `config/agents/projects/fs.yaml` — `PROJECT_SERVICE` указывал на мёртвые юниты (`fstrim` static, `jo-agent-fs` disabled, `nfs-common` masked) → заменён на реальные `fs-fed-port-node-f`, `fs-fed-relay-node-e`, `fs-fed-route-node-e` (проверено: все три существуют и `enabled`).

### 2.2 Политика моделей возвращена в канон

- `config/models.yaml` восстановлен из `4b1897e` (+ ссылка на сценарий) → `MODEL-POLICY: OK`.
- «Локально-первая политика» осталась отдельным файлом `config/models.arm-pending.yaml` и теперь **валидна при включении** (добавлены `escalate` и `limits`): раньше её включение сломало бы гейт политики, и агенты молча уехали бы на встроенные умолчания. Заголовок файла честно объясняет: что предлагает, почему не активно (сервер 2 `130.61.16.167` недостижим: порты 443/80 закрыты), какие цифры за этим стоят, как включить и как откатить.
- Решение о включении — за владельцем: это смена экономики (бесплатные локальные модели против бесплатных быстрых внешних).

### 2.3 Ложный тир `arena` убран

Живой эксперимент через шим (`127.0.0.1:9700`):

| Запрос | Ответ |
|---|---|
| `hermes-fast` | `tier=fast`, `provider=groq-gpt-oss-20b` |
| `hermes-arena` | `aios_tier=arena`, но `provider=groq-gpt-oss-20b`, `provider_tier=fast` |

Витрина обещала «arena.ai direct mode: claude-sonnet-4-5», а отвечал обычный fast-провайдер — ровно тот класс лжи, против которого написана прошлая волна «отвечает тот, кто реально ответил». Причины: балансер `/opt/octopus-aios-server.py` (строка 52) знает тиры `fast|reasoning|code|long_context|local`, слова `arena` в его коде нет; сервис на `8790/8791` — это REST-драйвер вкладки Arena через CDP (`/opt/orchestrator/arena_service/app.py`, авторизация `X-API-Key`), а не OpenAI-совместимый эндпоинт. Правка была незакоммиченной: она не попала в git и снята из рабочего дерева, а живой шим перезапущен — `/v1/models` теперь 6 алиасов, `arena` не рекламируется. Гейт в секции `[19]` фиксирует честный список тиров, чтобы витрина не разошлась с балансером снова.

### 2.4 Регрессионные гейты (чтобы это не вернулось)

- `tests/probe-chat.py` + `tests/run.sh` `[11]`: 9 проб адресации упоминанием — упоминание это адресат (одиночное, несколько подряд), `k=v` переживает срез, явный `handler` из конверта побеждает упоминание, сообщение из одних упоминаний уходит в `identity`.
- Новая секция `[19]`: словарь обработчиков не индексируется как список (и известный обработчик всё ещё выбирается по имени); упоминание срезается до выбора обработчика; `analysis` — запрос, а не константа; шим не рекламирует тир, которого нет у балансера; неактивный сценарий политики обязан называть себя неактивным.
- Гейтов стало 307 (было 290).

### 2.5 Служебное

- Удалены устаревшие бэкапы `agents/runtime.py.bak-mention-*`, `agents/runtime.py.bak-note-*` и `scripts/gen-project-agents.sh.bak` (копии — в страховочном архиве).
- `Documentation=file:/opt/hermes/docs/RUNBOOK.md /opt/hermes/agents/runtime.py` давал в журнале `Invalid URL, ignoring` → два `file:///`-URI в репозитории и в рабочем юните; systemd перечитан, юнит не перезапускался (`active since 06:39:45`).

## 3. Живые доказательства

- **Политика:** `MODEL-POLICY: OK`, sha256 `2e959b6aa2f3`, `тир по умолчанию: hermes-fast · агентов с тиром: 6 · эскалаций: 3`.
- **Шим:** `/v1/models` → 6 алиасов (`auto/code/fast/local/long/reason`); живой `ask` → `tier=fast`, `provider=groq-gpt-oss-20b`.
- **Обращение по упоминанию в общем канале:** опубликовано `@monitoring health` в канал `monitoring` (шина NATS, `hermes.chat.monitoring.normal`) → ответ агента: `monitoring.health → OK (1.16s)` с реальным отчётом (Prometheus `:9090`, цели 5).
- **Юниты:** 8/8 Hermes активны (`serve, gateway, agents, bus-bridge, metrics, shim, telegram-inbox, alert-poller`), dashboard `:9119` отвечает 302 (за паролем), шим `:9700` — 200.
- **Гейты:** `307/0/0` · `51/0` · secret-scan clean.

## 4. Открытые вопросы (нужны решения владельца)

1. **Включать ли «локально-первую политику».** Файл готов, включение — одна команда, откат описан. «За»: бесплатно. «Против»: 173,6 с на холодном кэше с полным промптом (поэтому факты для local урезаны до 900 знаков), сервер 2 недостижим.
2. **Arena как источник frontier-модели.** Тиром в балансере это не решается: нужен OpenAI-совместимый фасад над `/opt/orchestrator/arena_service` и провайдер с собственным тиром, который честно называет себя в ответе. Отдельная задача, не однострочник.
3. **Адресация по узлу** `@node-arm-llm/monitoring status` не резолвится: узел `node-arm-llm` на шине живой (`msgs=16`), но локальный рантайм отвечает `agent node-arm-llm/monitoring has no handler 'status'` — честный FAIL вместо маршрутизации на другой узел. Поддерживаемая форма разговора в общем канале сегодня — `@<agent-id>`; кросс-узловая адресация — отдельная задача.
4. Из прошлого отчёта и не сделано (чужое, ждёт «да»): клонировать `liza` (алерт настоящий) и поставить лимит памяти `octopus-browser-chromium`.

## 5. Оговорки

- Цифры 173,6 с / 0,5 с взяты из комментария автора правки 19.09 в `agents/models.py`, а не из моего прогона. Мои живые замеры: детерминированный обработчик `health` — 1,16 с (модель не вызывалась); шим `hermes-fast` — ответ за секунды.
- Юниты Hermes не перезапускались, кроме `hermes-shim` (иначе тир `arena` остался бы в живом ответе): это единственный способ убрать его из работающего процесса.
- Чужие проекты на узле (octopus, madworld, logistics, ipfs, browser, monitoring) не затрагивались.
